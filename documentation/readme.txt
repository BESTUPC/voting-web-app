1. baixar-se i installar node js https://nodejs.org/en/download/ (win) o fen servir l'script d'installacio(ubuntu)
2. baixar-se i installar mongodb http://stackoverflow.com/questions/20796714/how-do-i-start-mongo-db-from-windows
3. reiniciar ordinador
4. npm install dintre la carpeta code per instalar els moduls
5. copiar el arxiu googlecredentials.pswd (pinned message a telegram) a la carpeta code
6. a la carpeta C:/Program Files/MongoDB/Server/3.4/bin executar mongod (hauria de sortir "waiting for connections on port 27017")
7. node app.js dintre la carpeta code per executar la app (hauria de sortir "App listening on port 3000! Connected succesfully to server")
8. al chrome anar a localhost:3000
